// Берёт Треугольное место для точки x
// Отзеркалит на другую сторону треугольника соответственно, если
// x перешло через границу b/2
// b - Размер треуголька(по дефолту n(Диапазон от 1 до n значений a))
//

double triangulation(double a, double b){
if(a <= b/2){return a;}
return b-a;
}